IDX Exchange – Property Search Platform

A full-stack Zillow/Redfin-style property search application built with:

Node.js + Express (REST API backend)

MySQL (Dockerized) database

React frontend

Pagination, filtering, validation, indexing, and performance optimization
